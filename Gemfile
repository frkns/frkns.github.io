# Optional: only needed to preview the site locally.
# GitHub Pages builds the site automatically on push without this.
#   bundle install && bundle exec jekyll serve
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
