---
title: Hello worlds
---

This is the first posts. Writing one is just dropping a Markdown file in
`_posts/` named `YYYY-MM-DD-title.md` with a `title:` line at the top.

Inline math works: $E = mc^2$, and so do subscripts like $x_1 + x_2$.

Block math too:

$$
\sum_{i=1}^n i = \frac{n(n+1)}{2}
$$

Normal Markdown is all here — **bold**, *italic*, `code`, [links](/), and:

- lists
- like
- this
